services:
  signwall:
    build: .
    container_name: signwall
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      # NAS 正式上線前請改成自己的老師密碼；不要把正式密碼提交回 GitHub。
      - TEACHER_PASSWORD=${TEACHER_PASSWORD:-1234}
